# Server
PORT=3001
FRONTEND_ORIGIN=http://localhost:3000

# Mail destination
CONTACT_TO=Angelo_renovates@hotmail.com

# SMTP settings
# Defaults are Outlook/Hotmail-compatible.
SMTP_HOST=smtp-mail.outlook.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Optional explicit From header (defaults to SMTP_USER)
SMTP_FROM=
